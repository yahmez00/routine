import styled from 'styled-components';

const Input = styled.input`
    background: #a0aec0;
    color: #1a202c;
    padding: 0.5em;
    border: none;
    border-radius: 5px;
`;

export default Input;
